* text=auto eol=lf

# The PvP report templates are generator output: GitHub keeps them out of the
# language stats and collapses them in diffs.
src/content/pvp/*/index.template.html linguist-generated
