// Viewport width thresholds (px), shared by the JS viewport detectors
// (useBreakpoint, useBottomSheet). The layout-switching CSS media queries
// (bottom-sheet mode, grid column stacking) mirror these — keep them in sync.
export const MOBILE_MAX_WIDTH = 480
export const TABLET_MAX_WIDTH = 768
